spring:
  application:
    name: concurrentlabs
  datasource:
    url: jdbc:postgresql://localhost:5440/concurrentlabs
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: false
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true